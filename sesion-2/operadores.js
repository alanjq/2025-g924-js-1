// Operadores lógicos
/**
 * && AND
 * || OR
 * ! NOT
 * ?? NULLISH
 */
// AND
console.log( (7-2 == 6) && (5+1==6) );

// OR
console.log(  (7-1 == 6) || (5+1==6) );

// ! NOT
console.log( (7-2 != 6) && (5+1==6) );

// ?? NULLISH
console.log( null ?? (undefined) ?? ("derecho") );
